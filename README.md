# geolocation-app

How to install: 

Npm install the frontend folder 
Npm install the backend folder 

Run the frontend 
Run the backend

Search away!

Approach:

I tried to keep the code and styling light in order to focus on the task. The backend section seems to be the most important section so things like the data and the DB were important parts.

The styling is tasteful but not heavy. 